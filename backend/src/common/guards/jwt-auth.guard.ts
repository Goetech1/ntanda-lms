/**
 * Re-export of the real JWT Auth Guard from the auth module.
 * This file kept for backward compatibility with any imports from common/guards.
 * Prefer importing directly from modules/auth/guards/jwt-auth.guard.ts.
 */
export { JwtAuthGuard } from '../../modules/auth/guards/jwt-auth.guard';

