/**
 * Re-export of the real Roles Guard from the auth module.
 * This file kept for backward compatibility with any imports from common/guards.
 * Prefer importing directly from modules/auth/guards/roles.guard.ts.
 */
export { RolesGuard } from '../../modules/auth/guards/roles.guard';

